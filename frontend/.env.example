# Copy this file to .env.local and adjust values. Never commit .env.local.
# NEXT_PUBLIC_* variables are baked into the browser bundle at build time.

NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
